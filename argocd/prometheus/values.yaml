grafana:
  enabled: true
  ingress:
    enabled: false 
  
  adminUser: admin
  adminPassword: prom-operator
  
  defaultDatasourceEnabled: true
  defaultDatasourceUrl: https://prometheus.lab.member87.xyz

  grafana.ini:
    auth:
      disable_login_form: true
    auth.anonymous:
      enabled: false

prometheus:
  ingress:
    enabled: false 
  prometheusSpec:
    externalUrl: https://prometheus.lab.member87.xyz
    url: https://prometheus.lab.member87.xyz

alertmanager:
  ingress:
    enabled: false 
